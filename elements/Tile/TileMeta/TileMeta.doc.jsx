/* eslint-disable */
import React from 'react';
import Component from './TileMeta';
import PropTypes from 'prop-types';
import asType from '../../../proptypes/as';
import classNameType from '../../../proptypes/className';

/**
 * Renders a Tile Content display wrapper.
 * 
 * @composedWith UiWrapper
*/
function TileMeta(props) {
  return <Component {...props} />;
}

export default TileMeta;
