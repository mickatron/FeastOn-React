/* eslint-disable */
import React from 'react';
import Component from './MessageFooter';
import PropTypes from 'prop-types';
import asType from '../../../proptypes/as';
import classNameType from '../../../proptypes/className';

/**
 * Renders a footer display wrapper for the `Message` component.
 * 
 * @composedWith UiWrapper
*/
function MessageFooter(props) {
  return <Component {...props} />;
}

export default MessageFooter;
